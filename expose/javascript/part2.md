1. Line 12 prints 3 since the length of the array  of prices containing 100,200, and 300 is 3. The variable i is incremented 3 times, starting at 0 and ending at 3 (which is the lenght of prices.length), thus printing 3.
2. Line 13 prints 150. This is because var discountedPrice is declared as var and is thus overwritten by the last value in the second index of prices multiplied by .5. That is, 300*0.5, which is why 150 is printed.
3. Line 13 prints 150. We start with declaring finalPrice = 0 as a var. When i=0, discountedPrice is 100*(1-0.5)=50. finalPrice then  is overwritten and becomes 50. When i=1, discountedPrice is 200*(1-0.5)=100. finalPrice is then overwritten and becomes 100. When i=2, discountedPrice is 300*(1-0.5)=150. Finally, finalPrice is then overwritten and becomes 150. 
4. The function returns an array of discounted prices, discounted=[ 50, 100, 150 ]. For each iteration of the for loop, the values in the prices array are multiplied by 0.5. Each resulting value is then rounded to the nearest integer after it is multiplied by 100 and divided by 100, then each are pushed into the discounted array. 
5. The code causes an error because i was declared using let. Because variables declared as let are only block-scoped, the values of i only exist within the for loop and does not exist outside of it, resulting in an error. 
6. For the same reasoning as number 5, the code causes an error because discountedPrice is declared using let, so it only exists within the block of the for loop. 
7. Line 14 prints 150. This is because the let delaration allows the finalPrice variable to exist within the whole function block since it was declared at the beginning. It equals 150 since the previous values of 0 at the initial declaration is overwritten by 50, then by 100, then finally by 150. The logic of the overwriting is the same as question 3. 
8. Similar to number 6, the function returns an array of discounted prices, [ 50, 100, 150 ] since discounted=[ 50, 100, 150 ]. For each iteration of the for loop, the values in the prices array are multiplied by 0.5. Each resulting value is then rounded to the nearest integer after it is multiplied by 100 and divided by 100, then each are pushed into the discounted array. This works since the discountedPrice variable is only used in the block it is declared in (the for loop) and the same could be said with the i variable since they are both declared using let. Since discounted and finalPrice are declared at the top of the function using let, they are accessible throughout the whole function block, resulting in a proper return of the discounted array. 
9. Just like question 5, this code results in an error since i is declared with let. It only exists in the scope that it is declared in, the for loop in this case, so it is inaccessible and does not exist in line 11.
10. 3 is printed by line 12 since we declare the variable length using the const keyword, it is constant and takes the value of the length of the prices array [100, 200, 300], which is 3.
11. Similar to number 6 and 8, the function returns an array of discounted prices, [ 50, 100, 150 ] since discounted=[ 50, 100, 150 ]. For each iteration of the for loop, the values in the prices array are multiplied by 0.5. Each are pushed into the discounted array. This works since although the discountedPrice variable is declared as const (note const values cannot be reassigned), during each iteration of the for loop, a new discountedPrice variable is made. The discounted array is declared with const but is updated with each new value of discountedPrice and works properly since there is no reassignment. The i variable works fine since it is declared using let and is only accessed and used in the for loop block. 
12. A. To access the value of the name property, we use student.name, giving Sarah. B. To access the value of the Grad Year property, we use student["Grad Year"], giving 2022. C. To call the function for the greeting property, we use student.greeting(). To access the name property of the object in the Favorite Teacher property, we use student["Favorite Teacher"].name. E. To acess index zero in the array of the courseLoad, we use student.courseLoad[0].
13. A. '3'+2 gives 32 since 3 is declared as a string, 3 and 2 are concatenated together, giving 32. B. '3'-2 gives 1 since the minus sign casts '3' into a number, resulting in 3-2, giving 1. C. 3+null gives 3 since we are adding 3 with an absent value. D. '3' + null gives 3null since '3' is a string and null is a primitive datatype, 3 is declared first as a string and null is then casted into a string as well, and the two are concatenated together. E. true+3 gives 4 since 3 is a number and there is a plus sign, true is casted into 1, leading to 1+3 = 4. F. false+null gives 0 since there is a plus sign, false is casted into 0 and since null is just the absence of value, we just have 0. G. '3'+undefined gives 3undefined. 3 is a string and because of the plus sign, undefined is casted into a string and the two are concatenated. F. '3'-undefined gives NaN or not a number. The minus sign casts 3 into a number and since undefined is not a number, their difference is not a number, NaN.
14. A. '2' > 1 gives the output of true since although '2' is a string, the presence of > converts it into a number, and 2>1 is true. B. '2' < '12' gives the output false since they are both strings, so they are compared lexicograpbically. '2' comes after the '1' in '12', so '2' < '12' is false. C. 2== '2' gives the output true since the presence of == forces '2' to be a number, 2==2 is true. D. 2==='2' gives the output false since === is a strict equality, '2' is not casted into a number and it stays a string. A string is not equal to a number, so false is the output. E. true == 2 is false since true becomes 1 due to the ==. 1 != 2, so false is the output. F. true === Boolean(2) since Boolean(2) is true for any non-zero number, so true === true is true by strict equality.
15. == and === both compare values, but using == will cast and change values to the same type before comparing if needed and possible to see if they are equal. === is a strict equality comparison that compares type and value without type conversion.
16. for (let val in statistics){
    if(val.startsWith('r') || statistics[val]% 2 !==0){
        console.log(statistics[val]);
    }
};
17. The result is [ 2, 4, 6 ]. modifyArray takes two parameters an array and a function callback, or simply a function passed as an argument to another function. The arguments in this case are the array [ 1, 2 ,3 ] and doSomething, which doubles the value of a number. The for...let loop iterates through the array and doubles each quantity then finally pushes it into newArr, which we then return.
18. Note that setInterval() repeatedly calls a function
setInterval(() => {
    let d = new Date();
    let time = d.toLocaleTimeString();
    console.log(time);

}, 1000);
19. The output of the code is:
1
4
3
2
console.log(1) prints 1. In line 3, setTimeout schedules 2 to be printed after 1000 ms. In line 4, setTimeout schedules 3 to be printed after 0 ms. console.log(4) prints 4. Since lines 2 and 5 are not printed with setTimeout, they are printed first. 